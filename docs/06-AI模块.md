# AI模块

本文档详细描述AI玩家的决策逻辑和行动实现。

## 一、模块概述

AI模块负责控制AI玩家的游戏行为，包括摸牌、出牌、碰/杠/吃/胡决策等。AI采用简单策略，模拟真人玩家的基本行为。

## 二、AI玩家创建

### 2.1 AI玩家属性

```javascript
{
    id: 'ai_' + Date.now() + '_' + seatIndex,  // AI唯一ID
    username: 'AI小明' | 'AI小红' | 'AI小刚' | 'AI小丽',
    avatar: '🤖' | '🎮' | '💻' | '🎯',
    voice: 'female01' | 'female02' | 'male' | 'male02',  // 动态分配
    socket: null,               // AI没有Socket
    ready: true,                // AI默认准备
    isBot: true,                // 标记为AI
    // ... 其他属性与真人玩家相同
}
```

### 2.2 AI语音分配

```javascript
// 动态分配AI语音，避开已有玩家的语音
const allVoices = ['female01', 'female02', 'male', 'male02'];
const usedVoices = this.players.map(p => p.voice);
const availableVoices = allVoices.filter(v => !usedVoices.includes(v));
const aiVoice = availableVoices.length > 0 
    ? availableVoices[0] 
    : allVoices[seatIndex % 4];
```

---

## 三、AI主行动逻辑

### 3.1 aiAction(aiPlayer)

**功能**: AI玩家主行动入口

**代码位置**: server.js:2333-2453

**处理流程**:

```javascript
aiAction(aiPlayer) {
    if (!this.gameRunning) return;
    
    if (this.gameState.turnPhase === 'draw') {
        // 摸牌阶段
        if (this.gameState.deck.length === 0) {
            this.endRound('draw', -1, -1, false, false);
            return;
        }
        
        // 摸牌（包含补花）
        const tile = this.drawTileForPlayer(aiPlayer, false);
        if (!tile) {
            this.endRound('draw', -1, -1, false, false);
            return;
        }
        
        // 广播AI摸牌
        this.broadcast('ai_draw', {
            playerIndex: aiPlayer.seatIndex,
            playerName: aiPlayer.username,
            flowerCount: aiPlayer.flowers.length
        });
        
        // 检查自摸（只有敲牌后才能自摸）
        if (aiPlayer.isQiao && this.canHu(aiPlayer.hand, aiPlayer.melds)) {
            this.endRound('hu', aiPlayer.seatIndex, -1, true, false);
            return;
        }
        
        // 检查暗杠（优先于加杠）
        const anGangActions = this.checkAnGangAvailable(aiPlayer);
        if (anGangActions.length > 0) {
            this.executeAnGang(aiPlayer, [anGangActions[0]]);
            return;
        }
        
        // 检查加杠
        let shouldJiaGang = false;
        for (const meld of aiPlayer.melds) {
            if (meld.type === 'peng' && meld.tiles && meld.tiles.length > 0) {
                const pengTile = meld.tiles[0];
                if (pengTile.type === tile.type && pengTile.value === tile.value) {
                    shouldJiaGang = true;
                    break;
                }
            }
        }
        
        if (shouldJiaGang) {
            // 执行加杠
            // ... 加杠后摸牌继续
            return;
        }
        
        this.gameState.turnPhase = 'discard';
        
        // AI出牌
        setTimeout(() => {
            if (this.gameRunning) {
                this.aiDiscard(aiPlayer);
            }
        }, 500 + Math.random() * 500);
        
    } else if (this.gameState.turnPhase === 'discard') {
        this.aiDiscard(aiPlayer);
    }
}
```

---

## 四、AI出牌策略

### 4.1 aiDiscard(aiPlayer)

**功能**: AI出牌决策

**代码位置**: server.js:2456-2536

**策略**: 优先出孤张或边张

```javascript
aiDiscard(aiPlayer) {
    if (!this.gameRunning) return;
    if (!aiPlayer || !aiPlayer.hand || aiPlayer.hand.length === 0) return;
    
    const hand = [...aiPlayer.hand];
    let discardTile = null;
    
    // 统计每种牌的数量
    const counts = {};
    hand.forEach(t => {
        const key = `${t.type}_${t.value}`;
        counts[key] = (counts[key] || 0) + 1;
    });
    
    // 策略1：优先出孤张
    for (const tile of hand) {
        const key = `${tile.type}_${tile.value}`;
        if (counts[key] === 1) {
            // 检查是否是边张（没有相邻牌）
            const leftKey = `${tile.type}_${tile.value - 1}`;
            const rightKey = `${tile.type}_${tile.value + 1}`;
            if (!counts[leftKey] && !counts[rightKey]) {
                discardTile = tile;
                break;
            }
        }
    }
    
    // 策略2：没有孤张就出第一张
    if (!discardTile) {
        discardTile = hand[0];
    }
    
    // 执行出牌
    const tileIndex = aiPlayer.hand.findIndex(t => t.id === discardTile.id);
    aiPlayer.hand.splice(tileIndex, 1);
    aiPlayer.discards.push(discardTile);
    aiPlayer.hand = sortTiles(aiPlayer.hand);
    
    this.gameState.lastDiscard = discardTile;
    this.gameState.lastDiscardPlayer = aiPlayer.seatIndex;
    this.gameState.lastDrawnTile = null;
    this.gameState.gangShangPao = false;
    
    // 检查AI是否听牌，自动敲牌
    if (!aiPlayer.isTing && !aiPlayer.isQiao) {
        const tingTiles = this.getTingTiles(aiPlayer.hand, aiPlayer.melds);
        if (tingTiles.length > 0) {
            aiPlayer.isTing = true;
            aiPlayer.isQiao = true;  // AI听牌后自动敲牌
            
            this.broadcast('player_qiao', {
                playerIndex: aiPlayer.seatIndex,
                username: aiPlayer.username,
                voice: aiPlayer.voice || 'female01'
            });
        }
    }
    
    // 广播出牌
    this.broadcast('tile_discarded', {
        playerIndex: aiPlayer.seatIndex,
        tile: discardTile,
        tileName: getTileName(discardTile),
        isAI: true
    });
    
    // 检查其他玩家动作
    this.checkActionsAfterDiscard(discardTile, aiPlayer.seatIndex);
}
```

### 4.2 出牌策略详解

| 优先级 | 策略 | 说明 |
|--------|------|------|
| 1 | 孤张边张 | 只有1张且没有相邻牌 |
| 2 | 孤张 | 只有1张的牌 |
| 3 | 第一张 | 默认出排序后的第一张 |

**示例**:
```
手牌: [一万, 一万, 二万, 三万, 五万, 八万, 八万, 八万, 三条, 五条, 九条, 东风, 南风]

分析:
- 一万: 2张 (对子)
- 二万: 1张 (有相邻一万、三万)
- 三万: 1张 (有相邻二万)
- 五万: 1张 (孤张，无相邻)
- 八万: 3张 (刻子)
- 三条: 1张 (孤张，无相邻)
- 五条: 1张 (孤张，无相邻)
- 九条: 1张 (孤张边张)
- 东风: 1张 (孤张)
- 南风: 1张 (孤张)

优先出: 九条 (孤张边张)
```

---

## 五、AI动作决策

### 5.1 aiDecideAction(aiPlayer, action)

**功能**: AI决定是否执行动作（碰/杠/吃/胡）

**代码位置**: server.js:2539-2570

**策略**:

```javascript
aiDecideAction(aiPlayer, action) {
    // 策略：胡必胡，杠必杠，碰概率50%，吃概率50%
    
    if (action.actions.includes('hu')) {
        // 胡牌必胡
        action.resolved = true;
        action.action = 'hu';
    } else if (action.actions.includes('gang')) {
        // 杠牌必杠
        action.resolved = true;
        action.action = 'gang';
    } else if (action.actions.includes('an_gang')) {
        // 暗杠必杠
        action.resolved = true;
        action.action = 'an_gang';
    } else if (action.actions.includes('peng') && Math.random() > 0.5) {
        // 碰牌50%概率
        action.resolved = true;
        action.action = 'peng';
    } else if (action.actions.includes('chi') && Math.random() > 0.5) {
        // 吃牌50%概率
        action.resolved = true;
        action.action = 'chi';
        // 随机选择吃牌选项
        if (action.chiOptions && action.chiOptions.length > 0) {
            action.selectedChiIndex = Math.floor(Math.random() * action.chiOptions.length);
        }
    } else {
        // 其他情况过
        action.resolved = true;
        action.action = 'pass';
    }
    
    // 检查是否所有动作都已处理
    if (this.gameState.pendingActions.every(a => a.resolved)) {
        clearTimeout(this.gameState.actionTimeout);
        this.resolveActions();
    }
}
```

### 5.2 决策策略表

| 动作 | 决策 | 概率 |
|------|------|------|
| 胡 | 必胡 | 100% |
| 杠（明杠） | 必杠 | 100% |
| 暗杠 | 必杠 | 100% |
| 碰 | 随机 | 50% |
| 吃 | 随机 | 50% |

---

## 六、AI暗杠处理

### 6.1 aiHandleAnGang(aiPlayer)

**功能**: AI处理暗杠

**代码位置**: server.js:2283-2300

```javascript
aiHandleAnGang(aiPlayer) {
    if (!this.gameRunning || !this.gameState.pendingAnGang) {
        return;
    }
    
    const anGangOptions = this.gameState.pendingAnGang.anGangOptions;
    if (anGangOptions && anGangOptions.length > 0) {
        // AI选择第一组暗杠
        const option = anGangOptions[0];
        this.gameState.pendingAnGang = null;
        this.executeAnGang(aiPlayer, [option]);
    } else {
        this.gameState.pendingAnGang = null;
        this.notifyCurrentPlayer();
    }
}
```

---

## 七、AI加杠处理

### 7.1 aiHandleJiaGang(aiPlayer)

**功能**: AI处理加杠

**代码位置**: server.js:2303-2320

```javascript
aiHandleJiaGang(aiPlayer) {
    if (!this.gameRunning || !this.gameState.pendingJiaGang) {
        return;
    }
    
    const jiaGangOptions = this.gameState.pendingJiaGang.jiaGangOptions;
    if (jiaGangOptions && jiaGangOptions.length > 0) {
        // AI选择第一个加杠
        const option = jiaGangOptions[0];
        this.gameState.pendingJiaGang = null;
        this.executeJiaGang(aiPlayer, [option]);
    } else {
        this.gameState.pendingJiaGang = null;
        this.notifyCurrentPlayer();
    }
}
```

---

## 八、AI接管机制

### 8.1 离线玩家AI接管

当真人玩家断线时，AI自动接管：

```javascript
// removePlayer 中
if (this.gameRunning && this.gameState.currentPlayerIndex === player.seatIndex) {
    // 正好轮到断线玩家，AI立即接管
    setTimeout(() => {
        if (this.gameRunning && player.offline) {
            this.aiAction(player);
        }
    }, 500);
}
```

### 8.2 超时AI接管

倒计时结束未准备的玩家由AI接管：

```javascript
forceStartNextRound() {
    this.players.forEach(p => {
        if (!p.isBot && !p.ready && !p.offline) {
            p.aiTakeover = true;
            p.ready = true;
        }
    });
    
    this.broadcast('ai_takeover_status', {
        readyStatus: this.getReadyStatus()
    });
    
    setTimeout(() => {
        if (!this.gameRunning) {
            this.startGame();
        }
    }, 500);
}
```

### 8.3 玩家恢复控制

```javascript
takeoverAI(socketId) {
    const player = this.players.find(p => p.id === socketId);
    if (!player) return { error: '玩家不存在' };
    
    if (!player.aiTakeover) {
        return { error: '你没有被AI接管' };
    }
    
    player.aiTakeover = false;
    
    // 通知恢复控制
    if (player.socket) {
        player.socket.emit('takeover_success', {
            message: '已恢复控制权！',
            seatIndex: player.seatIndex
        });
    }
    
    // 广播状态
    this.broadcast('player_takeover', {
        username: player.username,
        seatIndex: player.seatIndex
    });
    
    // 如果正好轮到这个玩家，设置出牌超时
    if (this.gameState.currentPlayerIndex === player.seatIndex && 
        this.gameState.turnPhase === 'discard') {
        this.setDiscardTimeout(player);
    }
    
    this.broadcastGameState();
    
    return { success: true };
}
```

---

## 九、AI思考延迟

AI行动有随机延迟，模拟真人思考：

```javascript
// AI思考延迟配置
const GAME_TIMEOUT_CONFIG = {
    AI_THINK_DELAY_MIN: 500,   // 最小延迟 0.5秒
    AI_THINK_DELAY_MAX: 1000,  // 最大延迟 1秒
};

// 使用
setTimeout(() => {
    this.aiAction(aiPlayer);
}, 500 + Math.random() * 500);

// AI决策延迟
setTimeout(() => {
    this.aiDecideAction(player, action);
}, 500 + Math.random() * 1000);
```

---

## 十、AI行为流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AI行为流程                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  notifyCurrentPlayer()                                              │
│       │                                                             │
│       ├── 是AI玩家?                                                 │
│       │   ├── 是 → 延迟后执行 aiAction()                           │
│       │   └── 否 → 等待真人玩家操作                                │
│       │                                                             │
│       ▼                                                             │
│  aiAction(aiPlayer)                                                 │
│       │                                                             │
│       ├── turnPhase === 'draw'?                                     │
│       │   ├── 是 → 摸牌                                             │
│       │   │   ├── 检查自摸 → 是 → endRound()                       │
│       │   │   ├── 检查暗杠 → 是 → executeAnGang()                  │
│       │   │   ├── 检查加杠 → 是 → executeJiaGang()                 │
│       │   │   └── 否 → 进入出牌阶段                                 │
│       │   │                                                         │
│       │   └── turnPhase === 'discard'?                             │
│       │       └── 是 → aiDiscard()                                 │
│       │                                                             │
│       ▼                                                             │
│  aiDiscard(aiPlayer)                                                │
│       │                                                             │
│       ├── 分析手牌，选择出牌策略                                    │
│       │   ├── 优先出孤张边张                                       │
│       │   ├── 其次出孤张                                           │
│       │   └── 默认出第一张                                         │
│       │                                                             │
│       ├── 检查听牌 → 是 → 自动敲牌                                 │
│       │                                                             │
│       └── checkActionsAfterDiscard()                                │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  notifyPendingActions()                                             │
│       │                                                             │
│       ├── 是AI玩家?                                                 │
│       │   └── 是 → 延迟后执行 aiDecideAction()                     │
│       │                                                             │
│       ▼                                                             │
│  aiDecideAction(aiPlayer, action)                                   │
│       │                                                             │
│       ├── 有胡? → 必胡                                              │
│       ├── 有杠? → 必杠                                              │
│       ├── 有碰? → 50%概率碰                                        │
│       ├── 有吃? → 50%概率吃                                        │
│       └── 否则 → 过                                                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 十一、AI优化建议

当前AI采用简单策略，未来可优化方向：

### 11.1 出牌策略优化
- 考虑牌型组合（顺子、刻子优先级）
- 考虑防守（避免点炮）
- 考虑听牌效率

### 11.2 动作决策优化
- 根据手牌情况决定是否碰/杠
- 考虑其他玩家的出牌习惯
- 根据牌局进度调整策略

### 11.3 牌型判断优化
- 识别更好的胡牌牌型
- 优先追求高番牌型
- 考虑花数最大化

